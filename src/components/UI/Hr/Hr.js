import React from "react";
import { View } from "react-native";

const hr = props => {
  return (
    <View
      style={{
        borderBottomColor: "#ccc",
        borderBottomWidth: 1
      }}
    />
  );
};

export default hr;
